# Trimming dataframes
# Does not work with zipped (txt.gz) files


# Select only row 3 from file.txt. Pipe the output to less
sed ‘3q;d’ file.txt | less -S


# Gets rid of row 1 and 2
sed '1,2d' file.txt
